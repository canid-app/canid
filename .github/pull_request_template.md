## What & why

<!-- What changed, and what problem it solves. -->

## How you tested it

<!-- e.g. `npx wrangler dev --persist-to /tmp/canid-wrangler` + the flows you exercised. -->

## Checklist

- [ ] No new dependencies, build steps, or backend.
- [ ] `CACHE` bumped in `service-worker.js` if any cached asset changed.
- [ ] FAQ copies / sample-card fragment kept in sync if touched (see
      "Things kept in sync by hand" in the README).
- [ ] New platform: conservative regex, brand icon in `js/icons.js`, FAQ
      platform list updated.
